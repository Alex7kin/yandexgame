# Claude Code local project config (editor/preview tooling, not part of the game)
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db

# Node / Cloudflare Worker tooling
node_modules/
.wrangler/
/package-lock.json   # root has no Node project; the bot/ lockfile is tracked

# Local Worker secrets — NEVER commit
.dev.vars
*.dev.vars
